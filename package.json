{
  "name": "kameletron",
  "version": "0.9.1",
  "description": "Bash customization app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager .  --overwrite --platform=linux --arch=x64 --prune=true --out=dist --icon=64x64.png"
  },
  "keywords": [
    "electron",
    "bash",
    "colors"
  ],
  "author": "Paulo J. Martins",
  "license": "GPL-3.0",
  "dependencies": {
    "fix-path": "^2.1.0",
    "jquery": "^3.2.1",
    "shelljs": "^0.7.8"
  },
  "devDependencies": {
    "electron": "^1.6.11",
    "electron-packager": "^8.7.2"
  }
}
